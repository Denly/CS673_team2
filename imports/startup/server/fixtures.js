import { Control } from '/imports/api/control/control.js';
var minimumUserSize = 10;
var currentUserCount = Meteor.users.find().count();

	//	Control.serverNewUser('699173800246359');
		console.error('yooo');

/*if (currentUserCount < minimumUserSize)
{
	for (i = currentUserCount; currentUserCount < minimumUserSize; i++)
	{


	}
}*/
